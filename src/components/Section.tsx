import { Section as SectionProps } from "@/constant";
import React from "react";

const Section: React.FC<SectionProps> = ({ desc, href, image, title }) => {
  return <div>Section</div>;
};

export default Section;
